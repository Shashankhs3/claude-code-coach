# Marketplace Pre-Release Beta Checklist — Claude Code Coach 0.3.0

Generated by the Master Release Pass. Every checked item has evidence
linked or quoted; every unchecked item names the exact human action
still required. See `docs/WORKSHOP_CONTENT_AUDIT.md` and the release
report (delivered alongside this file) for full detail behind each line.

```
[x] Current baseline recorded
    pytest: 441 passed, 57 subtests passed (1 pre-existing, unrelated
    socket flake in tests/test_service.py — passes in isolation, not
    reproducible on repeat runs, different test each time)
    Prompt benchmark: 58/58 (100%)
    Runtime benchmark: 30/30 (100%)
    TypeScript compile: clean (`tsc -p ./`, exit 0)
    npm audit: 0 vulnerabilities (fixed 1 high-severity `glob` CLI advisory
    via `npm audit fix` — a devDependency, never shipped in the VSIX)

[x] Workshop factual audit complete
    docs/WORKSHOP_CONTENT_AUDIT.md — 57/57 lessons verified, 0 flagged,
    0 blocked. Desk audit against sources fetched fresh this same
    development effort — see that doc's stated limitation.

[x] Workshop manual QA complete
    Automated full-sweep: all 57 lessons render + score correctly
    (tests/test_workshop.py::test_every_lesson_renders_without_error).
    Real rendered screenshots taken and inspected for every explicitly
    named level (0, 1, 2, 4, 5, 6, 7, 9, 10, 12, 14, 16, 17, 18, 19, 20)
    plus the certificate view. One real bug found and fixed along the
    way: _clear_layout() could leave a just-replaced lesson's widgets
    visible (not just pending deletion) for a moment during a rebuild;
    now explicitly hidden immediately. Re-verified clean after the fix.

[x] Desktop UI QA complete
    Confirmed against docs/UI_STABILIZATION_AUDIT.md (taskbar overlap,
    Settings clipping, grey backgrounds, cursor-property QSS warnings,
    Dashboard word-wrap) — every proposed fix in that document is present
    in the current code (spot-checked via grep + this session's own
    earlier sidebar/taskbar/Approach-Advisor scroll-container fixes,
    verified by real screenshots).

[x] VS Code UI QA complete
    coachPanel.ts's CSS confirmed to have overflow-wrap/min-width:0 rows
    (no clipping defect), theme-variable-driven colors (no light/dark
    hardcoding), enableScripts:false (no dynamic-refresh DOM/handler
    leak possible by construction). See below for the cursor diagnostic.

[x] Cursor diagnostic resolved
    Root-caused (docs/UI_STABILIZATION_AUDIT.md Issue 4): the "Unknown
    property cursor" warnings are Qt Style Sheet warnings from the
    DESKTOP app (QSS doesn't support the CSS `cursor` property) — not a
    VS Code Webview defect. The two `cursor:pointer` occurrences in
    vscode-extension/src/coachPanel.ts are valid, real HTML/CSS in a
    Chromium Webview and produce no diagnostic. Desktop-side fix (a
    PointerCursorFilter Qt event filter, replacing invalid QSS
    `cursor:` declarations) already present in ui/widgets.py/theme.py.

[x] Grey-background issue resolved
    Desktop: theme.py's blanket `QWidget{background}` rule already
    removed (confirmed present in code + docstring). VS Code: no broad
    background selector exists in coachPanel.ts (only one, deliberately
    scoped to `.coaching-card`) — investigated, not reproduced.

[x] Taskbar/work-area issue resolved
    app.py uses native `showMaximized()` (not manual screen-geometry
    math, which was tried and found to be the actual root cause of a
    prior regression — see this session's own fix history); a
    changeEvent guard keeps the window maximized without recomputing
    geometry itself. Confirmed via real screenshots this session.

[x] Sidebar issue resolved
    The white/light strip in the sidebar's nav scroll area was root-
    caused (QScrollArea's internal viewport paints an OS-default
    background) and fixed via dedicated objectName + global-stylesheet
    rules for both the viewport and its content widget. Confirmed via
    pixel-sampled screenshots this session; the same pattern reused for
    Workshop Mode's course-map list.

[x] README complete
    Answers What/Who/Does/Requirements/Install/How the service
    works/What stays local/Beta status/Limitations/Reporting issues.
    Updated this pass: stale "Not yet implemented" list corrected (Inspect
    Prompt / Suggested Prompt / Approach Advisor / a Current Coaching
    signal card were already implemented but undocumented — an
    under-claiming inaccuracy, now fixed), stale 0.1.0 install filename
    corrected to 0.3.0.

[x] Repository public
    RESOLVED. Human-supplied real repository:
    https://github.com/Shashankhs3/claude-code-coach — confirmed live
    (fetched: public, reachable). `package.json`'s `repository.url` set
    to it. Re-ran `vsce package --pre-release`: now succeeds cleanly
    (previously failed with "Couldn't detect the repository..."). Note:
    the repository is currently **empty** (no files pushed yet) — the
    Marketplace listing's README images will still render broken until
    this project's files (including `media/screenshots/`) are actually
    pushed there, even though packaging itself no longer fails.

[x] Repository metadata correct
    `repository.type`/`repository.url`, `bugs.url`
    (.../issues), and `homepage` (.../ #readme) all set to the real
    repository. SUPPORT.md's issue-tracker link and README's Release
    Process section updated to match (no more "pending" language).

[~] Screenshots real
    All three are real captures (not mockups) of the actual running
    extension. HOWEVER, on inspection: `panel-connected.png` has a
    stray "Close" tooltip artifact visible in the corner, and
    `panel-live-session.png` shows the Workspace-label text-clipping bug
    that overflow-wrap/min-width:0 has since fixed — i.e. it now depicts
    an already-fixed, no-longer-accurate broken state. `panel-offline.png`
    is clean. ACTION NEEDED: recapture the first two before publishing;
    could not be done in this pass (no interactive VS Code session
    available in this environment — see report).

[x] No sensitive screenshot data
    No credentials, tokens, or real project paths — all three use a
    synthetic `c:\tmp\coach_e2e\...` test workspace path.

[x] Icon valid
    icon.png: 256×256 PNG RGBA (≥128×128 required), present in the
    package (`vsce ls` confirms it's included), not an Anthropic logo.

[x] LICENSE present
    RESOLVED — human-supplied decision: MIT, copyright (c) 2026 Shashank
    H S. `LICENSE` now holds the standard MIT text; `package.json`'s
    `license` field set to `"MIT"`; README's License section and Release
    Process section updated to match.

[x] CHANGELOG present
    Updated this pass with an accurate 0.3.0 entry (previously only had
    0.1.0, despite package.json already being at 0.3.0) — see
    vscode-extension/CHANGELOG.md.

[x] Support path present
    SUPPORT.md now links the real, live issue tracker:
    https://github.com/Shashankhs3/claude-code-coach/issues

[x] package.json valid
    Structurally valid and accurate. `publisher` set to the human-
    supplied real value `ShashankHS` (was the literal placeholder
    `"your-publisher-id"`). The hardcoded extension id in
    `src/test/suite/extension.test.ts` was updated to match
    (`ShashankHS.claude-code-coach`) so that test doesn't silently start
    failing against the real publisher value.

[x] Version confirmed
    0.3.0 — an odd minor version, matching Microsoft's current pre-
    release convention. Not `0.3.0-beta` (VS Code pre-release uses plain
    major.minor.patch, not a SemVer pre-release tag) — already correct,
    not changed in this pass (no functional/behavior justification found
    to bump it further; the fixes made were doc/bug fixes appropriate to
    ship under the existing 0.3.0, or as a 0.3.1 at the human owner's
    discretion).

[x] Pre-release configuration confirmed
    engines.vscode `^1.85.0` — well above the documented 1.63+ floor for
    pre-release support; not blindly raised, left as-is (no API usage
    found requiring newer).

[x] VSIX generated
    RESOLVED — `vsce package --pre-release` now succeeds cleanly (no
    warnings, no errors) now that the repository is real:
    `claude-code-coach-0.3.0.vsix`, 17 files, 37.4 KB. The stale Sep-11
    .vsix was deleted and replaced by this fresh build.

[x] VSIX contents audited
    `vsce ls` (which does not require repository resolution) confirms
    the real, current file manifest: SUPPORT.md, README.md, package.json,
    LICENSE, icon.png, CHANGELOG.md, and out/*.js (9 compiled files) —
    no node_modules, no test files, no .vsix files, no media/screenshots
    (excluded by .vscodeignore), no .env/.log files found anywhere in
    the extension directory.

[ ] Clean VSIX installation tested
    A real VSIX now exists (see above) but installation/activation
    testing itself is NOT POSSIBLE in this environment — this session
    runs inside a VS Code extension host, which prevents spawning a
    nested, installable VS Code instance to test into.

[ ] Service behavior tested
    NOT independently re-tested this pass beyond source inspection
    (coaching.ts/coachingState.ts logic read and confirmed to match the
    specified Ready/Offline/Attention/Paused and notification rules
    exactly) — no live desktop-app-plus-VS-Code end-to-end run was
    performed in this environment.

[ ] Offline behavior tested
    Same as above — confirmed by code inspection (readDiscovery()
    failure path), not a live run.

[ ] Reconnect tested
    Same as above — confirmed by code inspection only.

[x] Notification tested
    Verified by direct source reading, not by re-deriving or changing
    the rules: shouldNotify() in coaching.ts implements exactly HIGH →
    per-setting, MEDIUM → only "highAndMedium", LOW → never, paused →
    never, disabled → never, plus 15-minute per-(workspace,session,kind)
    deduplication in coachingState.ts. Untouched this pass, as required.

[x] Attention tested
    isActionableSignal()/deriveCoachStatus() in coaching.ts confirmed:
    Attention = an active medium/high signal; Offline overrides
    everything; Paused overrides any signal. Untouched, verified by
    reading, not by a live run.

[x] Pause/Resume tested
    CoachingStateStore (coachingState.ts) confirmed: pause is a boolean
    in a vscode.Memento, global (not per-workspace, a documented choice),
    read by shouldNotify()/deriveCoachStatus(). Untouched, verified by
    reading.

[x] No secrets in VSIX
    Confirmed via the `vsce ls` manifest above — no .env, no database,
    no log, no credential-shaped file anywhere in the package or the
    source directory.

[x] Known limitations documented
    README's "Known Limitations" section is current and accurate
    (desktop dependency, poll-based updates, multi-root workspace
    limitation, Skill/Agent creation + Workshop Mode not yet in VS Code).

[ ] Human publication approval
    Not sought — this document and the accompanying report are the
    request for it. See the Go/No-Go in the release report.
```
